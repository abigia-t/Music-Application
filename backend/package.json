{
  "name": "music-app-backend",
  "version": "1.0.0",
  "description": "Backend API for music application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "docker:build": "docker build -t music-app-backend .",
    "docker:run": "docker run -p 5000:5000 music-app-backend",
    "docker:compose": "docker-compose up",
    "docker:compose:build": "docker-compose up --build"
  },
  "keywords": ["music", "api", "mern"],
  "author": "Abigia",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}